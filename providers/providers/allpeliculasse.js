async function getStreams(id, type, season, episode) {
    console.log("Proveedor recibido:", {
        id,
        type,
        season,
        episode
    });

    return [];
}

module.exports = {
    getStreams
};
